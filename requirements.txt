# 美股短线选股系统 MVP - Python 依赖

# 数据获取
yfinance>=0.2.36
requests>=2.31.0

# 技术分析
# `analyzer.py` 会优先尝试 pandas-ta；若不可用，则回退到本地 pandas_ta_compat.py。
# 当前项目验证环境使用 `ta` 作为兼容实现依赖。
ta>=0.11.0

# 数据处理
pandas>=2.0.0
numpy>=1.24.0

# HTML 解析 (用于获取 S&P 500 成分股列表)
lxml>=5.0.0
html5lib>=1.1

# 环境变量
python-dotenv>=1.0.0
